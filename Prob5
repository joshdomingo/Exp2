#include<iostream>
#include<conio.h>
using namespace std;
int main()
{
	int count;
	for (count = 1; count <= 30; count++)
	{
		if (count == 30)
			cout << "30";
		else if (count == 11) continue;
		else if (count == 13) continue;
		else if (count == 15) continue;
		else if (count == 17) continue;
		else if (count == 19) continue;
		else if (count == 21) continue;
		else if (count == 23) continue;
		else if (count == 25) continue;
		else if (count == 27) continue;
		else if (count == 29) continue;
		else
			cout << count << ",";
		
	}

	_getch();
	return 0;
}
